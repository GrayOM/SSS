## 프로젝트 목적

이 프로젝트는 보안 진단 업무를 보조하는 도구를 개발하기 위한 프로젝트이다.

주요 목적은 다음과 같다.

- 취약점 진단 결과 관리
- 점검 대상 관리
- 증적 정리
- 보고서 초안 생성
- 소스코드 점검 결과 정리
- 보안 점검 자동화

## 개발 기준

- 실제 업무에서 사용할 수 있는 구조로 설계한다.
- 기능 설명보다 구현 가능한 코드와 파일 구조를 우선한다.
- Backend는 FastAPI 또는 Python 기반을 우선 고려한다.
- Frontend는 HTML, JavaScript 또는 간단한 React 구조를 사용할 수 있다.
- DB가 필요한 경우 SQLite 또는 PostgreSQL 기준으로 제안한다.
- 인증 기능이 필요한 경우 세션, JWT, 권한 분리, 비밀번호 해시 저장을 고려한다.
- 파일 업로드 기능은 확장자, MIME Type, 크기, 저장 경로 검증을 포함한다.
- 외부 URL 요청 기능은 SSRF 방지 로직을 포함한다.
- 로그에는 민감정보가 남지 않도록 처리한다.
- API Key, Token, Cookie, Password는 코드에 직접 작성하지 않는다.

## 추가 자동화/보안 원칙

- PoC는 가능한 한 생성한다.
- executable PoC가 불가능하면 observational_poc를 생성한다.
- 그것도 불가능하면 manual_poc_plan과 이유를 남긴다.
- upload security, ZIP Slip 방어, path traversal 방어, file size limit, extension allowlist를 약화하지 않는다.
- ebs/nls/cia/nafal 같은 샘플 닉네임을 fixture/test/docs에 남기지 않는다.
- Windows 환경을 고려해 파일 읽기는 `encoding="utf-8"`을 명시한다.
- 테스트 실패를 고칠 때 테스트를 약화하지 말고 원인을 수정한다.
- 보안 제한을 풀어서 테스트를 통과시키지 않는다.

## 결과 화면 기준

결과 화면은 진단자가 보기 쉽게 구성한다.

취약점 결과는 다음 항목을 포함한다.

1. 취약점명
2. 위험도
3. 발생 위치
4. 영향도
5. 재현 절차
6. 증적
7. 개선 권고
8. 보고서용 문구

## 테스트 기준

기능 구현 후 가능한 경우 다음을 수행한다.

- 단위 테스트 추가
- 주요 API 정상 요청 테스트
- 잘못된 입력값 테스트
- 파일 업로드 제한 테스트
- 인증/인가 우회 가능성 점검
- 예외 상황 테스트

테스트 실행이 불가능한 환경이면, 실행하지 못한 이유를 명시하고 사용자가 직접 검증할 수 있는 명령어를 제공한다.

## 완료 기준

작업 완료 시 다음을 제공한다.

1. 변경 요약
2. 수정 파일 목록
3. 실행 방법
4. 테스트 방법
5. 보안 검토 포인트
